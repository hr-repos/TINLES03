name: your_project_name
description: A Flutter project integrating MQTT, Camera, and Joystick
publish_to: 'none' # Remove this line if you plan to publish

version: 1.0.0+1

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.9.4
  flutter_joystick: 0.2.1
  mqtt_client: ^9.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    # Add any assets you need here
    # - assets/

  # Add any custom fonts here
  # fonts:
  #   - family: CustomFont
  #     fonts:
  #       - asset: assets/fonts/CustomFont-Regular.ttf
